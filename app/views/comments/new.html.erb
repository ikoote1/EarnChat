<h1>Create a New Comment</h1>

<%= form_with(model: @post, url: user_posts_path(user_id: @current_user.id), local: true) do |form| %>
  <div>
    <%= form.label :text %>
    <%= form.text_area :text %>
  </div>

  <div>
    <%= form.submit "Create Comment" %>
  </div>
<% end %>
